#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n: is the number to be checked
 * Return: int (Success)
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	return (-n);
}

