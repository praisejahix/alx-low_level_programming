#include <stdio.h>
/**
 *main - prints all single digit numbers from 0 to 10
 *Return: 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
