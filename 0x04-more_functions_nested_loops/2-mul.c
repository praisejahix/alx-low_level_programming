#include "main.h"
#include <stdio.h>

/**
 * mul - returns the product of two integers
 * @a: the first no.
 * @b: the second no.
 * Return: the product
 */

int mul(int a, int b)
{
	return (a * b);
}
